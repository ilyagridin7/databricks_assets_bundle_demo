# This is a Databricks asset bundle definition for test.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: test
  uuid: ef46de80-a226-48f1-99ee-4bb6324da985

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405604777289863.3.azuredatabricks.net
    variables:
      catalog: dbt_formation
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://adb-7405604777289863.3.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/ilya.gridin@ragt.com to make sure we only have a single copy.
      root_path: /Workspace/Users/ilya.gridin@ragt.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dbt_formation
      schema: prod
    permissions:
      - user_name: ilya.gridin@ragt.com
        level: CAN_MANAGE
